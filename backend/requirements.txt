# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.12
python-dotenv==1.0.1

# Vector Store (Qdrant)
qdrant-client==1.15

# LLM & HTTP
groq==0.11.0
httpx==0.27.2

# Embeddings (text embeddings for RAG)
# NOTE: If using QDRANT_CLOUD_INFERENCE=true, you can skip these
#       Qdrant will generate embeddings server-side
sentence-transformers==3.2.1
transformers==4.46.2  # Required by sentence-transformers

# Document Processing
pypdf2==3.0.1  # PDF fallback parser
python-docx==1.1.2  # Word documents
pdfplumber==0.11.4  # Primary PDF parser

# Data Loading (for loading Wikipedia data)
datasets==3.1.0  # HuggingFace datasets
tqdm==4.66.5  # Progress bars
pandas==2.2.3  # Data manipulation

