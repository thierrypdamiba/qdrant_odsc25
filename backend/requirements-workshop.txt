# Workshop-Optimized Requirements
# This file contains the same packages as requirements.txt but organized by purpose
# Installation time: ~2-3 minutes

# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.12
python-dotenv==1.0.1

# Vector Store (Qdrant Cloud)
qdrant-client==1.15

# LLM & HTTP
groq==0.11.0
httpx==0.27.2

# Embeddings (for semantic search)
sentence-transformers==3.2.1
transformers==4.46.2

# Data Loading (for workshop - loads Wikipedia articles)
datasets==3.1.0
tqdm==4.66.5
pandas==2.2.3

# Document Processing (for file uploads)
pypdf2==3.0.1  # PDF fallback parser
python-docx==1.1.2  # Word documents
pdfplumber==0.11.4  # Primary PDF parser
pillow==11.0.0  # Image processing

